# Copy to `.env` and fill in. Never commit real secrets.
GROQ_API_KEY=

# Set to true to ingest default_csv at startup (bundled sample). Default false = clean slate until POST /ingest.
LOAD_DEFAULT_CSV_ON_STARTUP=false
